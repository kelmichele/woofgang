<div class="franchise-head v2">
	<div class="inside med">
		<!-- 998x500 -->
		<%= image_tag('fp-placeholder-long.jpg', class: 'img-responsive feat', alt: 'Woof Gang Bakery wrapped suv') %>
		<% if user_signed_in? && current_user.admin? %>
			<div class="seal"></div>
			<% end %>

		<div class="text">
			<h2>Become a Woof Gang Bakery Franchisee</h2>
			<h1><%= @page_title %></h1>
		</div>
	</div>
</div>
