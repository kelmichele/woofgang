<% @page_title = "Edit State" %>

<div class="ipage-header">
	<div class="inside narrow">
		<div class="full">
			<h1><%= @page_title %></h1>
		</div>
	</div>
</div>

<div class="full-sec">
	<div class="inside">
		<div class="full-btn">
			<%= link_to("[ Cancel and Return ]", locations_path, :class => 'back-link inline') %>
			<%= link_to "Delete This State", state_path(@state.id), method: :delete, data: { confirm: "Are you sure you want to delete?" }, class: "woof-link btn-danger" %>
		</div>
		<!-- <hr> -->
		<%= render 'form' %>
	</div>
</div>
